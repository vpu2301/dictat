// selectors.js — Sprint 13: DOM selectors for the grammar's affordances,
// owned by proposal/ (the pgm- vocabulary lives only here — tripwire-
// enforced). Step 06's jump-to-violation focuses the confirm button via
// this export instead of hardcoding the class elsewhere.

export const CONFIRM_BUTTON_SELECTOR = ".pgm-btn-confirm";
